# dependencies (only used locally for image conversion)
node_modules/
package.json
package-lock.json

# original heavy source images (WebP versions in assets/ are what ship)
Logo.jpg
*.png

# OS
Thumbs.db
.DS_Store